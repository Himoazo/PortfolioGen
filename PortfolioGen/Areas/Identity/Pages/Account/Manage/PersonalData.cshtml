@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Personlig data";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<div class="card shadow-sm">
    <div class="card-body">
        <h4 class="card-title mb-4">@ViewData["Title"]</h4>

        <p>Ditt konto innehåller personuppgifter som du har gett oss. På den här sidan kan du ladda ner eller radera dessa uppgifter.</p>
        <p><strong>Om du raderar dina uppgifter tas ditt konto bort permanent och kan inte återställas.</strong></p>

        <div class="d-flex gap-2 mt-4">
            <form id="download-data" asp-page="DownloadPersonalData" method="post" class="d-inline">
                <button class="btn btn-primary" type="submit">Ladda ner</button>
            </form>

            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">Radera</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
